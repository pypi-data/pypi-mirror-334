[tool.poetry]
name = "primapy-optimus-price"
version = "0.1.0"
description = "Automatic Optimization Library"
authors = ["Prima <pricing-octopus@prima.it>"]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"