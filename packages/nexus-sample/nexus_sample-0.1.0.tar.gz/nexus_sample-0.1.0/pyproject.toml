[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nexus_sample"
version = "0.1.0"
authors = [
  { name="thomas", email="thomas.in@osckorea.com" },
]
description = "nexus sample"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = "https://github.com/"