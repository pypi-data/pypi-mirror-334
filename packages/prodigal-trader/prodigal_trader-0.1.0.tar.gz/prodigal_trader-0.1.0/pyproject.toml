[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "prodigal-trader"
version = "0.1.0"
authors = [
    { name = "Prodigal AI", email = "outreach@prodigalai.com" }
]
description = "A sample Python package"
readme = "README.md"
requires-python = ">=3.7"
license = { file = "LICENSE" }
dependencies = [
    "requests",  # Add any required dependencies
]

[project.urls]
Homepage = "https://github.com/yourusername/my_package"
Repository = "https://github.com/yourusername/my_package"
