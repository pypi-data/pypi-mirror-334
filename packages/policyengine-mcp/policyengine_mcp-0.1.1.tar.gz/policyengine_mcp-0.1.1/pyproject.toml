[project]
name = "policyengine-mcp"
version = "0.1.1"
description = "PolicyEngine Model Context Protocol server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp",
    "policyengine",
]

[project.scripts]
policyengine-mcp = "main:start"
