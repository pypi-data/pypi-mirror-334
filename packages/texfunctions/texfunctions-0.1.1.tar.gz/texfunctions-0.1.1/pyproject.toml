[project]
name = "texfunctions"
version = "0.1.1"
description = "a package for generating tex in functional style (course assignment)"
authors = [
    {name = "Igor",email = "greafdorothy@yandex.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
