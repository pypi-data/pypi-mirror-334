[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "reallm"
version = "0.1.0"
description = "Reinforcement Agent Learning Language Model"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
license = {text = "Apache 2.0"} 
authors = [
    {name="Teïlo Millet", email="teilo.millet@gmail.com"}
]

[tool.hatch]
metadata-version = "2.4"

[tool.hatch.build.targets.wheel]
packages = ["rllm"]
