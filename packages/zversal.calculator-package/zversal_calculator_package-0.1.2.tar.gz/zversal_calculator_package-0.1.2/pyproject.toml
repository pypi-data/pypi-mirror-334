[project]
name = "zversal.calculator_package"  
version = "0.1.2"  # Increment version
description = "A simple calculator package"
authors = [{ name = "Nikhil Dehal", email = "nikkhil.zversal@gmail.com" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12"
dependencies = ["pymupdf"]

[tool.setuptools]
packages = ["calculator_package"] 