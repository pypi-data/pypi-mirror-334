# Add the following to your `~/.ssh/config` file:
#   Include /Users/gat/work/arena-2025-iap/secrets/config.ssh

# REMINDER: chmod 700 for folder, chmod 600 for keys

Host example-host
    HostName 127.0.0.1
    User root
    IdentityFile /path/to/gatpack/secrets/ssh/something.key
    Port 22

# 
    
# 