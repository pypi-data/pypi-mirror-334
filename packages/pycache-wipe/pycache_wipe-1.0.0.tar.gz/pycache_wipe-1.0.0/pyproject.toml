[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pycache-wipe"
version = "1.0.0"
description = "Simple utility to delete __pycache__ folders."
authors = [{ name="Gabriel Stanier", email="support@datadude.dev" }]
readme = "README.md"
license = {text = "MIT"}
dependencies = []

[project.scripts]
pycache-wipe = "pycache_wipe.main:main"
