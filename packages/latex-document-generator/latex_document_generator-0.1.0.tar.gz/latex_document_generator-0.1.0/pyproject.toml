[project]
name = "latex-document-generator"
version = "0.1.0"
description = ""
authors = [
    {name = "andrewbolotsky",email = "andrew04tlt@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "latex-components-generator (>=1.0.0,<2.0.0)"
]

[tool.poetry]
packages = [{include = "latex_document_generator", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
