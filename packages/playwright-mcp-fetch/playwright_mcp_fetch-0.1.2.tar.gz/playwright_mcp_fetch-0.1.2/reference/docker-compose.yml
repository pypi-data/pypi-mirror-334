version: '3'

services:
  fetch-mcp:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - TRANSPORT_TYPE=sse
      - fetch_html=Disable
      - DNListCheck=Enable
    restart: unless-stopped 