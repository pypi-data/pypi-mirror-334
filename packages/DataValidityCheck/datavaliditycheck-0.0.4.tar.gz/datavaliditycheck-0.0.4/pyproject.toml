[project]
name = "DataValidityCheck"
version = "0.0.4"
description = "A Python package for validating various types of data."
authors = [{ name = "Olajide Oluwafemi", email = "olajiderichard44@gmail.com" }]
license = "MIT"
readme = "README.md"

[project.urls]
homepage = "https://github.com/Data-Epic/Data-Validator-Oluwafemi-Olajide.git"
repository = "https://github.com/Data-Epic/Data-Validator-Oluwafemi-Olajide.git"

[tool.poetry]
packages = [{ include = "DataValidityCheck" }]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
