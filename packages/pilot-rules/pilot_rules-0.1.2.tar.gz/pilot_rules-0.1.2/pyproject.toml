[project]
name = "pilot-rules"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Andre Ratzenberger", email = "andre.ratzenberger@whiteduck.de" }
]
requires-python = ">=3.10"
dependencies = [
    "rich>=13.9.4",
    "tomli>=2.0.1",
]

[project.scripts]
pilot-rules = "pilot_rules:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
