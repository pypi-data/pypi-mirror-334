[project]
name = "jmobile-location-mcp-server"
version = "0.1.0"
description = "一个提供热点新闻头条信息功能的模型上下文协议（Model Context Protocol）服务器。"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
 "mcp[cli]>=1.4.1",
]
[[project.authors]]
name = "juhemcp"
email = "dev@juheapi.com"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
jmobile-location-mcp-server = "jmobile_location_mcp_server:main"
