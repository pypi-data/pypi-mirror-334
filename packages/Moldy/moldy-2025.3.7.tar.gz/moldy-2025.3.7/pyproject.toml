[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "Moldy"
version = "2025.3.7" # version
authors = [
  { name="Günthner" },
]
description = "I needed a quick templating engine for generating translated versions of files"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Environment :: Console"
]
# dependencies = []
keywords=["Template Engine"]

[project.scripts]
moldy = "moldy:command_entry_point"